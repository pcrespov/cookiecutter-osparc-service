dist: bionic
language: python
addons:
  apt:
    packages:
      - python3
      - "python3-pip"
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - "pip install -r requirements.txt"
script:
  - pytest -v -x
